release: bundle exec rails db:migrate
web: bundle exec rails server -p $PORT
release: bundle exec rails db:migrate
web: bundle exec rails server -p $PORT
release: bundle exec rails db:migrate
web: bundle exec rails server -p $PORT
